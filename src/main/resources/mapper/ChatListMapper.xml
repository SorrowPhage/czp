<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sorrowphage.czp.mapper.ChatListMapper">

    <select id="selectChatRelationship" resultType="com.sorrowphage.czp.entity.ChatList">
        select * from chat_list where own_id=#{ownId} and other_id=#{otherId}
    </select>
</mapper>
